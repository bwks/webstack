<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <title>{{ view.title() }}</title>
  </head>
  <body>
    <main>
      <h1>{{ view.title() }}</h1>
      <p>{{ view.message() }}</p>
    </main>
  </body>
</html>
